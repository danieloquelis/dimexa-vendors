class Numbers {
  ///Limit max when paginate
  static int maxLimit = 200;
  static int startPage = 1;
  static int zero = 0;

  ///Max days allow after request for sync
  static int maxDaysAllow = 1;
  static double appBarHeight = 72;
}